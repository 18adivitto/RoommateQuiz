using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BoolHub : MonoBehaviour
{
    public static bool doorAnimationIsDone = false;
    public static bool textIsUp = false;
    public static bool button1IsClicked = false;
    public static bool button2IsClicked = false;
    public static bool button3IsClicked = false;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
